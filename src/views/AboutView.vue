<script setup lang="ts"></script>

<template>
  <section class="about">
    <h1>Методология</h1>

    <h2>Как это работает</h2>
    <p>
      Приложение много раз (по умолчанию 10 000 раз) «проигрывает» оставшиеся матчи турнира
      случайным образом и смотрит, на каком месте команда оказывается в каждом таком прогоне.
      Доля прогонов, где команда попала, например, в топ-8, и есть оценка вероятности такого
      исхода. Уже сыгранные матчи не переигрываются — их результат фиксирован.
    </p>

    <h2>Модель для футбола</h2>
    <p>
      Количество голов каждой команды в матче моделируется распределением Пуассона. Ожидаемое
      число голов считается из силы атаки одной команды, силы обороны другой и преимущества
      своего поля:
    </p>
    <pre class="formula">λ_дома = среднее × атака_дома × оборона_гостей × преимущество_поля
λ_гостей = среднее × атака_гостей × оборона_дома</pre>
    <p>
      Из этих двух ожидаемых значений случайно разыгрывается конкретный счёт. Опционально
      включается поправка Диксона–Коулза, которая немного корректирует вероятность редких низких
      счетов (0:0, 1:0, 0:1, 1:1) — по умолчанию она выключена.
    </p>

    <h2>Модель для Dota 2 (The International)</h2>
    <p>
      Вероятность выиграть отдельную карту считается по рейтингу Эло обеих команд:
    </p>
    <pre class="formula">p = 1 / (1 + 10^(-(Rₐ - R_b) / 400))</pre>
    <p>
      Серии Bo1/Bo3/Bo5 разыгрываются карта за картой до тех пор, пока одна из команд не наберёт
      нужное число побед; Bo2 может закончиться вничью 1:1.
    </p>

    <h2>Неопределённость силы команд</h2>
    <p>
      Рейтинги команд не идеальны, поэтому в каждом отдельном прогоне к ним добавляется небольшой
      случайный шум (параметр σ в настройках турнира). Это отражает то, что реальная сила команды
      на момент матча немного отличается от нашей точечной оценки.
    </p>

    <h2>Тай-брейки</h2>
    <p>
      Если после всех матчей у нескольких команд одинаковое число очков, применяется
      последовательность критериев (разница мячей, забитые голы, голы на выезде и так далее — для
      еврокубков это официальный регламент УЕФА). Критерии проверяются по очереди, пока порядок не
      определится.
    </p>

    <h2>Источники данных</h2>
    <p>
      Сейчас составы, календарь и рейтинги команд — это иллюстративные заглушки: реальный доступ к
      внешним источникам (ClubElo, The Odds API, football-data.org, OpenDota и т.д.) в этой сборке
      не настроен. Подробный список подходящих источников и как их предполагается использовать —
      в <code>docs/strength-params.md</code> и README проекта.
    </p>

    <h2>Ограничения модели</h2>
    <ul>
      <li>Голы/карты считаются независимыми — модель не учитывает «игру по счёту», усталость к концу турнира и травмы в моменте.</li>
      <li>Формат для The International (Swiss-система и плей-офф на выбывание) пока не реализован — страница турнира показывает только рейтинги команд.</li>
      <li>Календарь еврокубков сгенерирован упрощённым алгоритмом (каждая команда получает нужное число уникальных соперников), а не настоящим алгоритмом жеребьёвки УЕФА по корзинам.</li>
      <li>Оценка силы команд не отражает реальные травмы, дисквалификации или смену состава — этим занимается страница «Сила команд», но её параметры пока не подключены к движку (см. <code>docs/strength-params.md</code>).</li>
    </ul>
  </section>
</template>

<style scoped>
.about {
  max-width: 70ch;
}
h1 {
  margin-top: 0;
}
h2 {
  margin-top: 28px;
  font-size: 1.05rem;
}
.formula {
  background: var(--bg-elevated);
  border: 1px solid var(--border);
  border-radius: var(--radius);
  padding: 12px 14px;
  overflow-x: auto;
  font-size: 0.9rem;
}
code {
  background: var(--bg-elevated);
  border: 1px solid var(--border);
  border-radius: 4px;
  padding: 1px 5px;
  font-size: 0.9em;
}
</style>
